import styled from "styled-components";

export const Center = styled.div`
	max-width: inherit;
    padding-left: 0px;
    padding-right: 20px;

	@media (max-width: inherit) {
		max-width: inherit;
		padding: 0 15px;
	}
`;
